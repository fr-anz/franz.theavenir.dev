{
  "name": "personal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader native",
    "build": "vite build --configLoader native",
    "format": "prettier --write \"src/**/*.{js,css}\" \"api/**/*.js\" \"*.{js,json,md,html}\"",
    "format:check": "prettier --check \"src/**/*.{js,css}\" \"api/**/*.js\" \"*.{js,json,md,html}\"",
    "check": "npm run format:check && npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
